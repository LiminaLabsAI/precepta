# Vendor licensing service deps (NOT the sovereign app image — kept separate so
# app/ stays lean; the app gains `cryptography` only in Phase 17 to verify keys).
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
cryptography>=42
