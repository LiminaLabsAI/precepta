# Changelog — 2026-08

- 2026-08-01 — FEAT-001 (key scope + cost budgets) BUILT + verified: api_keys gains subject_type, allowed_backends, allowed_models, cost_cap_daily, cost_cap_monthly (migrations). New `app/budgets.py` (key_usage table, timezone-aware daily/monthly windows via org timezone) records real spend (metering × pricing) and enforces caps (warn 80% / block 100% → 429). Per-key backend/model scope enforced in the gateway (block). `GET /v1/usage`. org timezone setting added. Console: create-key form provisions all 5 scope dimensions + both cost caps; key cards show subject/scope/cap badges + live today-spend; `/v1/usage` wired. 5 new tests (9 for FEAT-001 total); full suite 108/108; browser-validated (no JS errors).
