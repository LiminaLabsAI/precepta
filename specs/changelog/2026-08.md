# Changelog — 2026-08

- 2026-08-01 — FEAT-001 (key scope + cost budgets) BUILT + verified: api_keys gains subject_type, allowed_backends, allowed_models, cost_cap_daily, cost_cap_monthly (migrations). New `app/budgets.py` (key_usage table, timezone-aware daily/monthly windows via org timezone) records real spend (metering × pricing) and enforces caps (warn 80% / block 100% → 429). Per-key backend/model scope enforced in the gateway (block). `GET /v1/usage`. org timezone setting added. Console: create-key form provisions all 5 scope dimensions + both cost caps; key cards show subject/scope/cap badges + live today-spend; `/v1/usage` wired. 5 new tests (9 for FEAT-001 total); full suite 108/108; browser-validated (no JS errors).
- 2026-08-01 — FEAT-001 keys v2 BUILT + verified: dropped subject-type + team (manual/unverifiable per user); added per-key TOKEN caps (daily/monthly) alongside cost caps; real bell NOTIFICATIONS on warn(80%)/block(100%) via new `app/notifications.py` (deduped, /notifications + /notifications/read); key EDIT (PUT /v1/keys/{id}) and SUSPEND/REACTIVATE (auth-blocking). Console: create/edit modal (no team/subject, cost+token caps, Keep-current expiry on edit), card Edit/Suspend/Revoke actions + Suspended state, live bell feed + unread badge. 5 new tests (14 for FEAT-001); 113/113; browser-validated end-to-end (real notification fired from a tripped token cap). Logged ENH-001..005 (rotation, last-used, rate-limit, usage page, IP allowlist).
- 2026-08-01 — Dropped role from keys (user decision): a key is an app-level credential (defaults to user-level — can call the governed API, cannot perform admin actions; admin stays human-only via Console/SSO). Removed the Role picker from create/edit, role from card display + save body, and role from update_key/POST. Closes the leaked-admin-key risk. 113/113; browser-validated.
- 2026-08-01 — FEAT-002 (policy scope + editing) BUILT + verified: governance_policies gains scope_json (migration); scope_matches(key/backend/model — empty=all, AND across dims, unknown value with restriction = no match) enforced in the gateway pre-inference (request's key = principal.subject; backend/model known for explicit requests); create_policy accepts scope; update_policy edits + BUMPS version (change trail); PUT /v1/policies/{id}. Console: policy scope pickers reworked to Key(app)/Backend/Model (dropped team/role/subject + the "not yet persisted" note), Edit modal, cards show "Applies to: N key · N backend · vN". 6 tests; 119/119; browser-validated.
- 2026-08-01 — Removed the compliance-evidence report + named-regulation claims (DPDP/GDPR/HIPAA/SOC2) from the Console (user decision): we haven't certified against those regulations, and showing a "compliance report" implied assurance we can't back. Removed the button + modal + handlers; softened residency/retention copy to describe declared settings, not regulatory compliance. Audit log + zero-egress attestation remain (technical facts, not compliance claims). Backend /compliance/report endpoint left in place but no longer surfaced. 119/119; browser-validated.
- 2026-08-01 — FEAT-007 slice 1 (governance routing filter / Rule C) BUILT + verified: new `app/governance/sensitive.py` (sensitive_backends store: backend + hosting location + approved_by + approved_at). Sensitivity auto-derived from Stage-1 firewall (PII/PHI) + caller data_tag. Enforced in the gateway for explicit requests: sensitive → approved-backend-only, else BLOCK (403) + critical admin notification. Off-until-configured (empty approved set = no enforcement; firewall still redacts) so it's non-breaking. Endpoints GET/POST/DELETE /v1/routing/approved. Console: "Advanced routing — sensitive data" box made real — approve chips + compliance-grade location-confirm modal (shows boundary/model, records location), enforcement status line; the "Optimize automatically" toggle stays honestly tagged Preview·FEAT-007 (LLM router next). 5 tests; full suite 124/124; browser-validated end-to-end (approve→enforce→block→notify).
- 2026-08-01 — IMPLEMENTATION_PLAN.md: rewrote the Phase 10 section as a status-tracked, phase-wise development plan (9 phases: money foundations ✅ · access & keys ✅ · policy governance ✅ · smart routing 🔵 · cost optimization ⚪ · learning ⚪ · enterprise hardening ⚪ · deploy ⚪ · validation ⚪), reflecting live build state.
- 2026-08-01 — Phase 4 task 1 (router config in Settings) BUILT + verified: the platform-owner-only surface that configures where the router's own in-boundary model runs. New `app/adapters/secret/` (SecretStorePort adapter — SqliteSecretStore: put/get/is_set/delete; a dedicated `secrets` table, the seam for KMS/Vault later — keys never returned over the API). New `app/router/config.py` (persisted `router_config`: router_backend ollama|hf, hf_endpoint, hf_model; HF key stored via the secret store as `router.hf_key`, reported only as `hf_key_set` boolean; fail-closed — selecting `hf` requires both endpoint + key, validated before persist). New platform-owner gate in `app/adapters/authz` (`is_platform_owner`, `PRECEPTA_PLATFORM_OWNERS` env, default 123.sarang@gmail.com + admin@local for dev). Endpoints GET/PUT `/v1/router/config` (owner-only, 403 else; config change anchored to the tamper-evident chain); `/auth/me` now returns `platform_owner`. Console: owner-only "Router" settings tab — Ollama/HF backend cards, in-boundary endpoint + model fields, password key field with "key stored" status (never shows the value), HF switch blocked until endpoint+key present. 7 tests; full suite 131/131; browser-validated (owner sees tab, switch works, key hidden, non-owner 403).
- 2026-08-01 — Phase 4 task 2 (eval harness / FEAT-006) BUILT + verified: the LOCKED router-quality evaluator (Rule 11), built BEFORE the LLM router so the router has a fixed number to move. `tests/benchmarks/router_eval_v1.json` (12 frozen cases across easy/hard/latency, each with a reference answer + rubric); `judge.py` (in-boundary Ollama `llama3.2:3b` judge, temp=0, frozen prompt — sovereignty: the judge never leaves the boundary); `harness.py` drives the REAL routing path (`brain.decide` → `engine.execute`) and scores each answer → **scalar = mean answer-quality (0..1)**, with mean cost/req + p50 latency as guardrails (via `metering.meter`, TD-002). Scoring judges the actual answer (user's choice). CLI `python -m tests.benchmarks` = the backend-real run; deterministic stub-based unit tests keep CI green without models. **Baseline (rules brain, automatic, judge v1): scalar 0.854 over 12 cases (easy 0.955 · hard 0.726 · latency 0.933), cost $0/req local, p50 1079ms.** Honest caveat: 3B judge + small local backends = weak signal until a stronger in-boundary backend/judge is plugged in (same interface). 12 tests; full suite 143/143; eval ran live against Ollama.
- 2026-08-01 — Phase 4 task 3 (LLM intent-router / FEAT-007·A) BUILT + verified: `auto` requests can now be routed by a small IN-BOUNDARY model. New `app/router/intent.py` (`classify(query)` → {goal: cost|speed|quality, difficulty: easy|hard} via the router-config model — local Ollama default or the owner's in-boundary HF endpoint; cached by query-hash; fail-soft → None on any error). New `LLMBrain` in `app/router/brain.py` maps goal/difficulty → backend balancing cost+speed (quality/hard → strongest tier, speed → fastest, else cheapest), passthrough (balanced); explicit intents delegate to rules; classifier failure falls back to RulesBrain (marked in reason). Governance auto-path filter (Rule C, was a gap): `candidates()`/`engine.execute()` now honor an `allowed` set; the pipeline computes it from the approved-for-sensitive backends for sensitive auto requests and threads it through `run_inference` (main.py + mcp.py), so the LLM router AND failover can only pick approved backends — fail-closed 403 + notify when none is approved. "Optimize automatically" toggle wired to a real `optimize_auto` org setting (default OFF = safe; on = LLM router); Console copy de-Previewed. 15 tests (candidate filter, classification parse, cache+fail-soft, goal→backend mapping, explicit-delegate, allowed-honoring, fail-closed auto block); full suite 158/158. Live eval (PRECEPTA_BRAIN=llm): scalar holds 0.854/12 (p50 latency 676ms vs 1079 rules) — classification real, but with 2 undifferentiated tier-1 local backends the router can't beat rules (the "over-engineering for 2 similar backends" doubt, now empirical) and the 3B classifier emits invalid JSON ~half the time (fail-soft handles it). Browser-validated: toggle persists + flips brain live (llm↔rules), no JS errors.
- 2026-08-01 — Phase 5 · response cache (FEAT-003) BUILT + verified: new `app/cache.py` — reuse a prior answer instead of re-calling a model. Safe by default (OFF until admin enables; only temp=0 deterministic requests cached; SENSITIVE requests never cached/served; per-team scope). Cache hits stay governed (firewall+policy run first, hit is audited) and metered as usage with **$0 budget charge** (TD-002 `cache_hit`). Exact-match default; semantic opt-in via in-boundary Ollama `nomic-embed-text` embeddings + cosine ≥ threshold (default 1.0=exact). Every path FAIL-SOFT (TD-006) — any error → cache miss/skip store, never breaks inference. Integrated in `governed_chat` (lookup before inference, store after output-firewall); `model_str` threaded from main.py + mcp.py; metering cache-aware. Admin-only `GET /v1/cache/stats` + `POST /v1/cache/clear`. Console "Cache & compression" screen made real (toggle, live entries/hits/tokens-saved/cost-saved, semantic opt-in + threshold, clear). 6 tests; full suite 164/164. **Live-validated: identical temp=0 request 287ms → 11ms (26× faster) on 2nd call, 35 tokens saved, served without touching a backend.** Cost saved reads $0 on free Ollama (honest) — real $ against a priced backend.
- 2026-08-01 — Phase 5 · prompt compression (FEAT-005) BUILT + verified: new `app/compression.py` — shorten prompts before inference. Never-surprise stance: OFF by default; **baseline** mode is quality-safe (whitespace/formatting normalization only, user-role messages only, system/assistant untouched); **aggressive** "cost-saving mode" is a separate opt-in that also strips conservative politeness filler (lossy) and, when it shortens a request, surfaces it in `precepta.compression` AND fires a deduped admin notification. Fail-soft (TD-006) — any error passes the original through. Integrated in `governed_chat` in canonical order (firewall→policy→cache→**compress**→inference); billing follows the compressed prompt automatically. Admin-only `GET /v1/compression/stats`. Console compression card made real (baseline toggle + aggressive opt-in + live requests-compressed/tokens-saved). LLMLingua deferred until eval-gated (Rule 11). 6 tests; full suite 170/170. **Live-validated: filler-heavy prompt 14→5 tokens (9 saved, ~64%), meaning preserved, transparently reported, aggressive notification fired.** **→ Phase 5 (cache + compression) COMPLETE.**
- 2026-08-01 — Phase 6 · learning loop (FEAT-008) BUILT + verified: route smarter over time from real outcomes. New `app/learning.py` — every auto request leaves a **trace** (difficulty bucket, backend, latency, cost, fell_over); reward = explicit thumbs (+1/-1, dominant) + implicit (failover penalty, clean-completion base). `preference(difficulty)` returns the best-mean-reward backend once it has ≥MIN_TRACES(3) evidence and only if positive; new `LearnedBrain` wraps the LLM router and biases to that backend — **bounded by governance** (learned pick must be an eligible/allowed candidate) and fail-soft. OFF by default (safe); never pooled across customers. Brain selection: `learned` > `llm` > `rules`. Trace recorded in main.py for auto requests (not cache hits), `trace_id` returned in `precepta`. `POST /v1/feedback` (thumbs) + admin-only `GET /v1/learning/stats`. Console: Playground responses get 👍/👎 that teach the router; "Learn from usage" toggle wired to real `learning_enabled` (de-Previewed). Quality gated by the LOCKED eval harness (Rule 11). 5 tests; full suite 175/175. **Live-validated end-to-end through the UI: auto request → trace → thumbs-up in Playground → /v1/feedback → aggregate reward rose to 0.7.** **→ Phase 6 COMPLETE.**
- 2026-08-01 — Phase 7 · hardening (partial) BUILT + verified: **TD-004 stronger sensitivity** — `app/governance/firewall.py` rewritten with **validated** detectors (credit cards must pass Luhn → fewer false positives; a random 16-digit number is no longer treated as a card) plus India/DPDP identifiers (Aadhaar by format — err toward redaction since a wrong checksum would risk a false negative; PAN), IPv4, India mobile numbers, and expanded injection + output-leak patterns (DB URLs, RSA/EC private keys). Real ML NER (Presidio/spaCy, in-boundary) documented as the deferred next step. **TD-007 attestation scope** — `build_attestation` now enumerates the in-boundary **data stores** (cache, traces, secrets, audit, backends), each asserted local/in-boundary and listed only when the table exists, so the zero-egress attestation honestly covers the stores added this phase. **TD-006 fail-soft** already baked into cache/compression/router/learning. 6 new firewall tests + attestation assertions; full suite 181/181. **Remaining Phase 7 (deferred — each needs dedicated design, NOT rushed): FEAT-004 OpenGuard authZ (RBAC/ABAC + agent budgets), TD-003 streaming vs governance, TD-005 agent attribution, TD-008 alerts config.**
- 2026-08-01 — Phase 7 · TD-003 streaming vs governance BUILT + verified: `/v1/chat/completions` now honors `stream: true` with OpenAI-compatible SSE. **The streaming DECISION:** governance must see the ENTIRE output before any is released (the Stage-3 leak scan can't un-send a token), so we run the full governed pipeline first (firewall→policy→cache→compress→inference→firewall-out) and then stream the APPROVED result as SSE — client compatibility without ever letting an unscanned token escape. True token-passthrough streaming (windowed governance) is a deferred, explicit opt-in, never the default. Blocks stay plain JSON (client sees status + reason). Final chunk carries `precepta` governance meta + usage. `_sse_response` in main.py. 3 tests; full suite 184/184. **Live-validated: real SSE stream from Ollama (role→content deltas→stop+precepta→[DONE]); injection + stream=true → clean 403 JSON, not a broken stream.** Phase 7 deferred now: FEAT-004 OpenGuard authZ, TD-005 agent attribution, TD-008 alerts config.
- 2026-08-01 — Phase 7 · TD-005 agent attribution BUILT + verified: requests may carry who/what is calling — `workflow_id`, `run_id`, `step_name`, `agent_id`, and the standard OpenAI `user` — threaded through `governed_chat` into `PolicyCheckContext` and recorded in the **tamper-evident audit chain metadata** (not the FK-constrained audit_log columns) so agentic traffic is attributable; echoed back in `precepta.attribution`. Only non-empty fields are recorded, so ordinary human requests stay clean. 3 tests; full suite 187/187.
- 2026-08-01 — Phase 7 · TD-008 alerts config BUILT + verified: `notify()` now consults an admin alerts config before firing — master switch (`alerts_enabled`), minimum severity (`alert_min_severity`: info/warning/critical), and per-category toggles (`alert_budget`/`alert_sensitive`/`alert_compression`). System/health alerts follow only the master switch + severity (non-silenceable by category). Category derived from the notification type. Console: new Settings → "Alerts" tab (master toggle, min-severity select, category toggles). 5 tests; full suite 192/192; browser-validated.
- 2026-08-01 — Phase 7 · FEAT-004 OpenGuard authZ BUILT + verified: richer authorization behind the existing `AuthorizationPort` (DIP — swaps in for hardcoded RoleCheck, no core change). `app/adapters/authz/openguard.py`: **configurable RBAC** (role→permission set, persisted `authz_roles`, seeded to preserve prior behaviour — admin=*, auditor=read, user=chat+read); **ABAC** (a permission may carry a `when` clause, e.g. `{"team":"finance"}`, granted only if the principal matches); **agent budgets** (per-`agent_id` daily request cap in `agent_budgets`/`agent_usage`, ties to TD-005 attribution — enforced in the chat endpoint → 429 when exceeded, blocked call not counted). `get_authz()` now returns OpenGuard. Admin endpoints: GET/PUT `/v1/authz/roles/{role}`, GET/POST `/v1/authz/agent-budgets`. Console: Settings → Members & roles shows real roles/permissions + agent budgets (used/cap) + a set-cap form. A full external OpenGuard/OPA engine would slot behind the same port (this is the in-boundary V1). 6 tests; full suite 198/198. **Live-validated: agent cap 2/day → 3rd request 429, usage capped 2/2; browser-validated the access-control UI. → Phase 7 pending items ALL DONE (FEAT-004, TD-003, TD-004, TD-005, TD-007, TD-008, TD-006).**
- 2026-08-01 — BUG-001 FIXED + verified: multiple backends of the same provider type now coexist. Root cause — Console sent `provider=PROVIDER_OF[type]` (every HF → `"hf"`) and the store/registry key by that, so a 2nd HF model overwrote the 1st. Fix: backend **identity = a slug of the Name** you type (e.g. "HF Gemma 31B" → `hf-gemma-31b`), falling back to the type when unnamed; the POST `/v1/backends` endpoint now also takes a **tier** (1–3, clamped) so an added strong model can be auto-routed to. Console add-backend modal: shows the derived identifier under Name + a "Model strength" selector. 2 tests; full suite 200/200; browser-validated (added a 2nd HF backend "HF Gemma 31B" via the UI → both `hf` and `hf-gemma-31b` listed in Model Plane, no overwrite; the external one honestly flagged "Leaves your network").
- 2026-08-01 — ARCH: separated **Precepta's proprietary router model** (`.env` HF) from **customer Model Plane backends** (user clarification). Was conflated — `registry.py` registered the `.env` HF (`HF_BASE_URL/HF_API_KEY/HF_DEFAULT_MODEL`) as a *customer* inference backend `reg["hf"]`, so it showed in the Model Plane and answered customer inference. Now: (1) `registry.py` no longer registers `.env` HF at all — the Model Plane holds only customer backends (Console-registered + local Ollama); (2) `app/router/intent.py` `_target()` resolves the router's own model as **Console Router-config override → `.env` HF (Precepta's model) → local Ollama**. **SOVEREIGNTY flag documented in code:** the router model reads the customer's prompt to classify it, so in production it MUST be an in-boundary deployment of Precepta's model — a public URL (e.g. `router.huggingface.co`) leaks prompts and is a DEV shortcut only. 3 tests; full suite 200→(203 with new)/203; browser-validated: Model Plane shows only customer backends (hf/Qwen + ollama), `.env` Gemma gone from it; an auto request classifies via `.env` Gemma and is answered by a customer backend.
- 2026-08-01 — Model Plane UX fixes (user-reported) BUILT + verified: (1) **Location column removed** — it duplicated Boundary (both derived from `in_boundary`), causing confusion. (2) **Boundary is now editable** — click the pill to flip in/out-of-network; persists via new `POST /v1/backends/{provider}/boundary` (+ `store.set_boundary`, live-registry sync). (3) **Add-backend now defaults HF to external** (public API leaves the network; tick the box for a dedicated in-VPC endpoint) — `setNewBackendType`. (4) **"Set price" is a real mechanism** — clicking it (or an existing price) opens an inline editor (input/output per 1M) → `POST /v1/pricing`; the footer note updated. 4 tests (boundary toggle + set-price + robustness); full suite 205/205; browser-validated end-to-end (corrected `hf-gemma-4-31b` to "Leaves your network" + set a price → badge cleared). **Note:** the attestation's `config.all_in_boundary` now honestly reports `False` when a customer registers an external backend (sovereign mode still blocks routing to it) — attestation tests made robust to that; a future refinement could distinguish "registered-external-but-blocked".
- 2026-08-01 — Notifications: fixed confusing/dead alerts (user-reported). Root cause — **test pollution**: several tests fire real `notify()` calls (budget/sensitive/compression/alerts) into the shared dev DB, so meaningless test notifications (`alert-system-1`, body `b`, etc.) leaked into the operator's feed. Fixes: (1) new `tests/conftest.py` autouse fixture removes any notification a test creates while preserving pre-existing ones — no more leakage from ANY test; (2) cleared the polluted dev feed. Separately, notifications were **not clickable** — now each row is actionable: `notifDest(type)` maps type→screen (budget→Keys & budgets, sensitive→Policies, compression→Cache & compression), the row shows a "<dest> →" hint and `app.onNotifClick` navigates + marks read. System/health alerts remain informational (no destination). Full suite 205/205, feed stays empty after a run; browser-validated: fired a real compression alert → clear title/body + "Cache & compression →" hint → click navigates there.
- 2026-08-03 — BUG (user-reported): the audit view's **"Cache & compression" filter always showed nothing**. Root cause — cache hits were audited only as generic `chat.completion` governance checks (→ "Chat" filter), and compression wrote **no** audit event at all (only its savings table + a notification); the filter matches events whose resource contains `cache`/`compression`, which nothing produced. This also broke the screen's own promise ("every cache hit and every compressed prompt is audited"). Fix: `governed_chat` now anchors a distinct tamper-evident chain event on each cache hit (`resource="cache.hit"`, metadata: backend/exact/tokens+cost saved) and on each compression (`resource="compression"`, action=mode, metadata: saved/original/compressed tokens). 2 tests; full suite 207/207. Live + browser-validated: triggering a cache hit + aggressive compression makes the "Cache & compression" audit filter show real `cache.hit`/`compression` rows.
- 2026-08-03 — Playground UX (user-reported): (1) the chat **auto-scrolls to the newest message** — each render replaced the DOM and reset scroll to the top, so replies went off-screen; `render()` now pins `#pgScroll` to the bottom on the Playground. (2) **sample prompts stay handy** — they only showed on the empty state and vanished after the first message; added a compact "Try:" strip above the input. UI-only; browser-validated (atBottom=true, strip visible, no errors).
- 2026-08-03 — Sovereignty controls made REAL (user-reported: the "In-boundary-only routing" switch was display-only — flipping it did nothing, so requests to external backends stayed blocked and it looked broken). Root cause — `toggleControl` only flipped local UI state; `sovereign_mode` was read once at startup from `PRECEPTA_SOVEREIGN` with no runtime override or API. Fix (user chose "real runtime toggle, owner-gated"): new `app/controls.py` (`runtime_controls` table + `sovereign_enabled()` = runtime override else deploy default) consulted by EVERY enforcement path — `enforce_backend`, router `candidates` (RulesBrain/LearnedBrain/engine), attestation, `/health`, `/api`. Endpoints: `GET /v1/controls` (state + `can_edit`), `POST /v1/controls/sovereign` (**platform-owner-only**, writes a `controls.sovereign` audit event). Console: in-boundary routing is now a genuine owner-gated toggle — turning it OFF opens a "removes your zero-egress guarantee" confirmation; non-owners see it locked; a red banner shows when off. The other three controls are now HONEST (egress mirrors in-boundary, audit read-only always-on, residency shows the declared label) instead of fake toggles. 3 tests (+updated test_enforce_backend); full suite 210/210. **Live + browser-validated: external backend 403 → owner toggles off → 200 → restored → 403; confirmation modal + real-state display work.**
- 2026-08-06 — FEAT-011 v1 (cache & compression v2, per-endpoint) BUILT + verified (demo feedback #2/#3/#4). Moved from GLOBAL toggles to **per inference endpoint** config. New `app/features.py` — `endpoint_features` table keyed by endpoint (or `"auto"` for router-decided requests); accessors for cache on/strategy/threshold + compression on/mode. `cache.py`/`compression.py` refactored to take an `endpoint`; enforced in `governed_chat` via `config_key = req_backend or "auto"` (known pre-inference — no pipeline reorder); per-endpoint stats + savings (added `endpoint` column, migrated). `GET /v1/features` (per-endpoint config + stats + endpoint list) · `PUT /v1/features/{endpoint}`. Console: the Cache & compression screen is now **two tabs** (Cache | Compression), each listing every endpoint + an **Auto (router)** row, with real per-endpoint toggles + strategy dropdowns (cache: exact/semantic + threshold; compression: baseline/aggressive) and an honest disabled **"Bring your own — coming"** option (real BYO engine → FEAT-011b backlog). Old global org cache/compression settings no longer read (harmless). Updated 6 test files to the per-endpoint API + new per-endpoint tests; full suite 211/211. **Live + browser-validated: ollama cache ON → 2nd identical call cache=hit (10ms); Auto row cache OFF → both miss (per-endpoint enforcement proven); two-tab UI + BYO option render.**
- 2026-08-01 — Session handoff: wrote HANDOFF.md (full context for the next session — what's built, what's next, locked decisions, hard rules, how to run/verify); status.md points to it; memory refreshed. Main @ 77774cd, 124 tests.
- 2026-08-10 — **Phase 11 — Traces COMPLETE (FEAT-010).** Finished the remaining ~40% on `phase-12-smart-router`: (1) **agent sub-trace nesting (L2)** — an agent target's own reasoning (Agent Trace Contract) nests under the inference checkpoint; `traces.step` gains `substeps`, the pipeline reads `route_meta.agent_steps` → 'Agent responded' with the nested reasoning (honest when absent), Console renders an 'Agent reasoning (sub-trace)' block; browser-verified. (2) **Bill-back** — `traces.billback(team)` + `/v1/billback` + a Console panel: real cost + requests by app and by agent, team-scoped. (3) **Learning-loop reward capture** — confirmed already handled by FEAT-008 (explicit thumbs + implicit failover penalty; off by default); no redundant path added. +7 tests (301 → 308). Phase 11 now: every-step L1 traces + L2 agent sub-trace + live request log + bill-back — all real, honest empties, admin-only, team-scoped.
- 2026-08-10 — **Smart Router ACTIVATED in the live path.** For auto requests, `run_inference` now runs the Phase-12 two-stage router: it maps the raw intent to a catalog intent, scores the eligible **in-boundary** backends (quality/tier vs cost vs latency, + warm hook), picks the target, and produces the **scored candidate set** — fail-soft (any error keeps the existing brain's plan; the engine still does dispatch/failover/cost-gating). `route_meta` carries `candidates`+`intent`+`inferred`; the trace routing step records them in `step.extra`; the Console **Traces → Route tab** now renders the real "candidates considered" (target · quality · cost/1M · chosen), replacing the placeholder. Fix: sanitize non-finite cost/latency (`state.latency()` returns inf for unobserved backends — was breaking scoring normalisation + JSON). **Live-verified against Ollama:** an auto request scored ollama ($0, chosen) vs hf ($0.60) and routed to ollama; out-of-boundary hf-* excluded under sovereign mode. +11 tests; **314 passing**; browser-verified, 0 broken handlers. Router decision + agent dispatch remain behind their ports; live agent-target routing is the next activation step.
- 2026-08-10 — **Phase 12 — Smart Router BUILT + fully tested (Groups 0–5)** on `phase-12-smart-router`. G0: intent catalog (open registry) + scoring resolver (pure, deterministic: `w_quality·q − w_cost·cost − w_latency·lat + w_warm·warm`, normalised, returns the full scored candidate set + confidence floor) + RouteTarget/Agent-Trace-Contract + BUG-001 regression guard. G1: two-stage smart decision (intent→scored candidates→target, injectable/pure). G2: InferenceTarget (LiteLLM lazy adapter, pure in-boundary allowlist guard — no provider fallthrough) + AgentTarget (timeout, fail-soft, governed egress hand-off, honest sub-trace capture). G3: in-boundary toxicity output filter (opt-in `toxicity_filter` setting) + enforcement-timing map (pre/during/post-call). G4: `/v1/workflow` + read-only Console **Workflow** screen (fixed governance rails + intents→targets, auto-generated from config; browser-verified, 0 broken handlers). G5: FROZEN 7-scenario router evaluator (Rule 11, scalar 1.0) + open-core boundary (`app/router/OPEN_CORE.md`, Apache-2.0) enforced by a no-commercial-imports test. **+90 tests (211 → 301), all green**; 6 commits; TDD caught + fixed 4 issues (smartest weighting, unregistered setting, eval-runner floor, missing-field expectation). Nothing deleted. Next: finish Phase 11 (agent sub-trace, bill-back, learning capture).
- 2026-08-10 — **Traces backend BUILT + wired (FEAT-010 / Phase 11, L1)** on `feat/console-redesign`. New `app/traces.py`: in-boundary, team-scoped `traces` store + step contract `{name,decision,reason,status,ms,inferred?}`, `Trace` accumulator, `save/list_traces/get_trace/get_run/stats`, all fail-soft (capture never breaks inference). `governed_chat` instrumented — a step at every governed stage (firewall → sensitivity → policy → cache → compression → routing → inference → output) with plain-language reasons + honest `inferred` flag on router intent; persisted on every outcome (allowed/blocked/failed). API: `GET /v1/traces` (list+stats), `/v1/traces/{id}`, `/v1/traces/runs/{run_id}`, `/v1/traces/stats` — admin-only, team-scoped. Console Traces screen wired to real data: KPI row (cost per accepted task, acceptance rate, route mix; escalation + router-overhead honestly `—` until the smart router lands), run list, and a checkpoint waterfall with per-step duration bars, decision, and why. **Verified:** 211 tests pass; live traffic yields real traces (allowed 6-step journeys, injection blocked at firewall, PII redacted → `[EMAIL_REDACTED]`, run stitching); browser-validated, 0 broken handlers; team isolation holds. Remaining for the full Phase-11 design: agent sub-trace nesting + the candidate-set/escalation panels (need the Phase-12 smart router).
- 2026-08-10 — Console redesign from `Precepta Console.dc.html` (imported via the Claude Design MCP), branch `feat/console-redesign`. **Cache & compression** restyled to the design's two-tab layout — a "Smart router default" card (toggle switches: exact on / semantic off) + a per-endpoint strategy list — keeping real `/v1/features` wiring and honest empty states (no mock 31%/$41.20). **Traces** screen added (new nav item + `tracesView`): a 5-KPI scaffold showing honest em-dashes and a "No traces captured yet" empty state (per the design's own empty-state spec — `app/traces.py` backend does not exist yet), wiring the real Generate-attestation button + Learn-from-usage toggle. **Inference plane** left as-is: already the backend-real form of the design (the design's GPU-util/throughput/traffic-weight bars have no data source, so they're honestly omitted). Browser-verified against the running app: both new/changed screens render real data, a live cache/compression toggle persists to the backend, the attestation modal opens against `/attestation`, and all 9 screens render with 0 broken handlers. Shell unchanged (already matches the design). 2 commits; JS syntax clean.
- 2026-08-09 — Brainstormed **Phase 12 — Smart Router (+ workflow view)** (`specs/phases/phase-12-smart-router/`). Two-stage routing: **model brain** picks intent → **config resolver** maps intent→target; unified `RouteTarget` for models **and** in-premise agents (agent trace contract + governed egress + off-by-default cache); LiteLLM `InferenceTarget` adapter (MIT-only, in-boundary allowlist); BUG-001 unique-id fix; governance-first wiring; read-only workflow view ("show, don't configure"); **open-core (Apache-2.0)**; locked router eval (7 scenarios). Competitor (LiteLLM) research → positioning reframed off "routing" onto **sovereignty + in-boundary reasoning-traces + governed agents**; parity "must-add" items split across phases (12: many providers + load balancing, usage-by-tool/agent, enforcement-timing, toxicity filter · 11: live request log, bill-back · deploy: easy-install, auto-provision · backlog: rate-limit, faster gateway). Execution order: **Phase 12 → Phase 11 Traces (extended) → Phase 13 Workflow builder**. Added FEAT-013..021, ENH-008. Roadmap/backlog/status updated. No code yet (planning only).
- 2026-08-10 — **Phase 14 — Deploy: Sovereign Pilot COMPLETE** on `phase-14-deploy-pilot`. G3–G5 landed on top of the container/compose/in-boundary-models base: real **startup egress probe** (`app/sovereign/probe.py` — socket-connects to 1.1.1.1 + huggingface.co) recorded to the tamper-evident chain and folded into `build_attestation` (`egress_test: {result, verified}`); **one-command UX** — `deploy/up.sh` (build→up→health→print Console URL), `deploy/doctor.sh` (plain-English preflight), `deploy/README.md` (5-step runbook); in-product **Deployment** screen (new nav item + `/v1/deployment` live status: running, in-boundary models, real egress, sovereign) with a setup checklist (real done/todo) + copy-runbook; **`deploy/smoke.sh`** proves the deployed stack end-to-end. **Verified in the live sovereign stack: SMOKE PASS** — healthy control plane, a governed `auto` request routed to the in-boundary Ollama, the attestation's real probe reports `egress: blocked, verified`, and the app container genuinely cannot reach the internet (1.1.1.1 unreachable). Closes **FEAT-009 (pilot slice)** + **TD-009** (router/embeddings in-boundary by config + bundling, not a core rewrite). Helm/Postgres/Vault/air-gap/HA/SCIM deferred to the production package.
- 2026-08-10 — **Copilot BUILT (real, grounded, in-boundary)** — replaced the "coming soon" FAB placeholder with a working assistant. `app/copilot.py`: `gather_facts()` snapshots live state from the real stores (org, sovereign mode, endpoints + in-boundary flags, keys, policies, cache/compression, real egress probe, activity); `answer()` grounds an **in-boundary** model on those facts with a strict "answer only from FACTS, never invent" system prompt; fail-soft to a deterministic, still-grounded summary when no model is reachable. `POST /v1/copilot` (principal-guarded). Console FAB is now a real chat — starter questions, markdown answers, an "Answered in-boundary · <model>" badge, Enter-to-send, thinking state. **Live-verified in the sovereign stack:** grounded answers matched ground truth (1 endpoint; egress blocked) via in-boundary `llama3.2:3b`; UI round-trip renders answer + badge. +5 tests (`tests/test_copilot.py`); **327 passing**.
- 2026-08-10 — **Project-wide dummy-code audit** (no UI-without-backend). Full-source checks: **0** UI handlers referenced without a definition (91 `app.*` calls all resolve); **every** frontend `fetch`/`getJSON` path (41) maps to a registered backend route; **no** fabricated/mock data in the Python backend (the only "hardcoded/mock" grep hits are docstrings explaining the code *avoids* hardcoding — pricing owned-table, honest "not configured" SSO, no-store to prevent stale data); all **9** Console screens render live with **0** throws and **0** console errors. Two remaining placeholders are honest, copy-labeled deferrals, not fake data: the **BYO** cache/compression engine (FEAT-011b, P3) and the Overview **per-hour time-series** chart (added as ENH-009).
- 2026-08-11 — **Pilot test-feedback fixes** (user tested the running stack; 8 issues) on `phase-14-deploy-pilot`. (1) **Attestation "Generate" did nothing** — the modal was only mounted inside the hidden Audit screen; moved to the root render so it opens from Deployment/Traces/Overview. (2) **Playground hung** — it defaulted to the first backend alphabetically (the unreachable cloud `hf`); now defaults to a **healthy in-boundary** backend (else `auto`), adds an explicit "Auto — Smart router" option, a "Thinking…" indicator (with a ~20–30s cold-start note), and disables Send while in flight. (3) **Local llama cold-start ~25s** — set `OLLAMA_KEEP_ALIVE=-1` so models stay resident. (4) **"Test & connect" was partly fake** — removed the dead 700ms-always-success stub; added a **real** `POST /v1/backends/{provider}/test` (honest reachable/blocked/unreachable + reason), a per-endpoint **Test** button on the Inference plane, and the onboarding test now surfaces the real "why". (5) **Cloud endpoints showed a bare "down"** — now explained honestly ("outside your sealed boundary — approve its host under Settings → Egress"), and the **copilot** learned per-endpoint reachability + the egress allowlist so it can answer "why is my HF endpoint down". (6) **Approved-egress allowlist (owner-gated)** — NEW `app/sovereign/egress.py` (host allowlist, subdomain-aware), enforced in `enforce_backend` (out-of-boundary permitted only if its host is approved), `GET/POST/DELETE /v1/egress` (owner-only, audited), a **Settings → Egress** pane, and the attestation now reports posture `sealed`→`restricted` with the approved hosts. Sealed stays the default; `deploy/README.md` documents the restricted-egress opt-in. (7) **Deployment screen** now shows the runbook **inline, step-by-step, each command copyable** (per-step + whole-runbook Copy). (8) **Workflow removed from the nav** (duplicated Traces + settings; screen/endpoint kept, restorable). +7 tests (`tests/test_egress_allowlist.py`); **334 passing**; SMOKE PASS still green (sovereignty intact); all 9 screens render with 0 console errors; live-verified end-to-end (real test-connection, egress approve/revoke audited + attestation posture flip, Playground routes to ollama, attestation modal opens).
- 2026-08-11 — **Restricted-egress network path wired end-to-end** (approved cloud endpoints now actually connect) on `phase-14-deploy-pilot`. NEW **egress broker** `app/sovereign/broker.py` — a filtering CONNECT proxy that tunnels ONLY to owner-approved hosts (read live from an allowfile, subdomain-aware, deny-by-default). `egress.py` writes `/data/approved_egress.txt` on every approve/revoke + a boot sync; the broker re-reads it per connection (approve/revoke is instant). Opt-in overlay `deploy/docker-compose.egress.yml` (+ `RESTRICTED_EGRESS=1 ./deploy/up.sh`): adds `egress-proxy` on internal+egress and points the app's `HTTPS_PROXY` at it with `NO_PROXY` for internal hosts — **the app is NOT given the egress network**, so it still has no direct internet route. **Verified live end-to-end:** from inside the sealed app container an approved host (example.com) returns 200 *through the broker* while an unapproved host is refused (ProxyError); the direct egress probe STILL fails (1.1.1.1/huggingface.co unreachable) so the attestation stays `result: blocked, verified: true` with `posture: restricted` + the approved host listed; ollama still routes (NO_PROXY correct). The broker itself: approved host tunnels (200), unapproved refused (curl exit 56). Sealed default unchanged — SMOKE PASS. +4 tests (broker allowlist + allowfile sync); **338 passing**. Honest posture: "the app has no direct egress (verified); it reaches only owner-approved hosts, through an audited broker."
- 2026-08-11 — **Approve egress from the registered endpoint** (host + key come from the Inference plane, not retyped). The `/infra` snapshot now exposes each backend's derived `host` + `egress_approved` + `egress_approvable` (external, not-yet-approved). NEW `POST /v1/backends/{provider}/approve-egress` (owner-only, audited) approves **that endpoint's** host, taken from the URL you registered — the stored key is what the probe/routing already use. `egress.py` gains `is_internal_host()` + `is_approvable()` so the approve action is offered only for external hosts (a bare `ollama`/`*.internal`/private-IP is never "approvable"), decoupled from a possibly-mislabelled in-boundary flag. Console: the Inference-plane **Test** result now shows an **"Approve <host> for egress"** button when applicable, and Settings → Egress lists a **"From your registered endpoints"** section to approve each cloud host in one click. **Verified live:** the registered `hf` endpoint's host (`router.huggingface.co`) is derived, test reports `approvable:true`, one-click approve adds it (noted `endpoint 'hf'`) and flips `egress_approved`, while `ollama` stays internal/non-approvable. +3 tests; **340 passing**; SMOKE PASS.
- 2026-08-11 — **Edit + Delete for Inference-plane endpoints** (user couldn't edit line items). NEW `PUT /v1/backends/{provider}` (admin-only) edits a registered endpoint in place — URL, model, tier, boundary — and **keeps the stored key when the field is left blank** (never blanks a secret by omission); a provided key replaces it. `/infra` now also returns `base_url` + `tier` + `has_key` so the edit form prefills (the key is never returned). Console: every Inference-plane row gets **Edit** + **Delete** (Delete confirms, hits the existing DELETE); Edit reuses the add-endpoint modal in edit mode — prefilled from the registered endpoint, identifier locked ("delete & re-add to rename"), key placeholder "leave blank to keep the stored key". **Verified live:** editing `hf` (model+tier, blank key) persisted and kept the key; /infra exposes base_url/tier/has_key; modal prefills the real URL. +3 tests (`tests/test_backend_edit.py`: fields+key-kept, key-replace, 404/authz); **343 passing**; SMOKE PASS.
- 2026-08-11 — **Policy budget-overlap trimmed + Cache/compression screen restructured + demo seed.** (1) **Trimmed policies:** removed the duplicate **daily-token cap** (`max_tokens_per_day` — the API key's `token_cap_daily` is the single home for budgets) and the **unenforced "Max daily cost"** field from the policy engine (`policy.py`) + the Console form. Policies keep the calls/hour rate limit + governance checks; per-app spend/token budgets live on the key. Updated `test_phase3` accordingly. (2) **Cache & compression** (user raised the confusion twice): replaced the separate "Smart router default" toggle-card + per-endpoint list with **one consistent table** — the "Auto — Smart router" row is simply the first row with the *same* single dropdown (Off / Exact match / Semantic) as every endpoint, plus a plain-English intro ("the router chooses the model; this chooses the caching — two separate things"). BYO stays a clearly-separate "coming soon" note, never a selectable option. Same for compression (Off / Baseline / Aggressive). (3) **Demo seed:** `app/demo_seed.py` + `deploy/seed-demo.sh` create idempotent, real sample **policies** (block foreign LLM providers · 120 req/hr rate limit · flag untagged requests (warn) · scan output for PII) and **keys** (mobile-app $5/day+$100/mo · analytics-batch 2M tok/mo · partner-readonly auditor · internal-dev admin/never-expires) through the same stores the Console uses. **Verified live:** seeded 4 policies + 4 keys; cache screen renders one dropdown per row (Auto+endpoints), policy form shows only calls/hour. +3 tests (seed idempotent + budgets, with a cleanup fixture so demo policies never leak into other tests); **345 passing**; SMOKE PASS.
- 2026-08-11 — **"Smart router (auto-decide)" cache strategy — matched the Claude design + real backend.** The design's per-model cache table wanted a **Smart router (auto-decide)** option where the system picks exact vs semantic vs skip *per request* — built for real, not a label. `app/cache.py`: `decide_smart(messages)` → **skip** (time-sensitive/personalized phrasings — don't reuse a stale answer), **exact** (short/lookup-style — semantic could serve a wrong near-match), **semantic** (longer natural-language question); `effective_strategy()` resolves it and `lookup`/`store` honour it (skip = no store, no lookup). `features.py` accepts `cache_strategy: "smart"` + new `cache_strategy()` accessor; **smart is now the product default** (`_DEFAULTS` cache on + smart + threshold 0.9). Console **Cache & compression** rebuilt to the design: a "Default cache strategy" card (router-routed requests) + a "Cache strategy by inference model" table with **friendly model names** (Ollama · vLLM cluster · Neysa · Hugging Face endpoint) and the dropdown **Smart router (auto-decide) / Exact match / Semantic / Skip**; compression mirrors the layout. **Verified live:** a short prompt (`2+2?`) auto-decided *exact* and returned `cache: hit` on the 2nd call; screen matches the design screenshot. +5 tests (`test_cache_smart.py`) + updated `test_cache`; **350 passing**; SMOKE PASS.
- 2026-08-11 — **Deploy clone URL + copilot navigation + smart compression.** (1) The in-product **Deployment runbook** now starts with a "Get the code" step showing the real **`git clone <repo> && cd precepta`** command — `repo` comes from `/v1/deployment` (`PRECEPTA_REPO_URL`, default the GitHub URL), copyable like the rest; README + .env.example updated. (2) **Copilot can navigate the app** — `app/copilot.py::detect_nav()` recognises "take me to / go to / open … <page>" and returns a `nav` action (fast, no model call); the Console executes `app.setScreen(nav)` and the copilot stays open to keep chatting (e.g. "take me to the deployment page" → moves to Deployment). (3) **Smart compression** — parallel to cache: `compression.decide_smart()` picks skip/baseline/aggressive per request by prompt length; `features` accepts `compression_mode: "smart"` (+ accessor), the pipeline honours it (skip = no compression), and it's now the **default**; the Console compression tab gains the **Smart router (auto-decide)** option in the same two-section design. **Verified live:** copilot navigated Overview→Deployment (panel stays open); deploy step shows the real clone URL; compression tab shows Smart. +8 tests (smart compression + copilot nav); **354 passing**; SMOKE PASS.
- 2026-08-11 — **Brainstormed Phase 15 — AI Provider Integration API** (`specs/phases/phase-15-provider-api/`). Reframed the "public API" phase off generic management CRUD onto **AI-provider integration** (LiteLLM-style): `GET /v1/providers` + an **in-boundary model catalog** (`/v1/catalog/models` — capabilities/context/pricing) → register an **inference endpoint** (`/v1/endpoints`, renames `/v1/backends` with alias) → **governed `/v1/inference` + `/v1/embeddings`** (`/v1/chat/completions` kept as an OpenAI-compat alias). Auth = per-team keys + a **`manage` scope** (read-only/read-write); sovereignty ops (sovereign mode, egress, router) stay owner-only. Typed Pydantic → curated OpenAPI at `/docs`; in-product API page. **Vocabulary decisions:** drop "backend" → *endpoint*, "chat" → *inference*. Deferred: SDKs, webhooks/events, GraphQL, Terraform, per-key rate-limit. Numbered 15 (13 stays reserved for the Workflow builder). Roadmap/status/backlog (FEAT-022) updated; Phase 14 marked Done. 4 phase files written; no code yet.
- 2026-08-11 — **Phase 15 — AI Provider Integration API: BUILT (G0–G5)** on `phase-15-provider-api`. Governed, sovereign provider-integration API. **G0:** `manage` scope on keys (read-only/read-write; maps to admin/auditor, never owner — sovereignty stays owner-only), `Principal.scope`, `app/api/{errors,deps,schemas}.py` (one error envelope + `require_manage`/`require_auth` guards + Pydantic contracts), curated in-boundary `app/catalog.py` + `app/data/model_catalog.json` + best-effort `catalog_lookup` (honest None on miss). **G1:** `GET /v1/providers`, `/v1/providers/{type}`, `/v1/catalog/models` (typed, no external call). **G2:** renamed `/v1/backends`→**`/v1/endpoints`** (aliases kept), `GET /v1/endpoints` (manage-scoped) + enriched `/v1/models` (mode/context/capabilities via catalog + real pricing + health). **G3:** governed **`/v1/inference`** (aliases the chat handler) + **`/v1/embeddings`** (firewall PII-redact + injection block, in-boundary embedder, audited), curated **OpenAPI** with a Bearer security scheme at `/docs`, `docs/api/README.md` guide. **G4:** owner-gated management-key issuance via `POST /v1/keys` (scope) + an in-product **API** page (base URL, auth, endpoint list, copyable curl, link to `/docs`). **Vocabulary:** "backend"→endpoint, "chat"→inference. **Verified live:** 5 providers, 8 chat catalog models, enriched endpoints (ollama matched; hf honest "unknown"), 768-dim governed embeddings (in-boundary, audited), OpenAPI has bearerAuth + the new paths; SMOKE PASS. +26 tests across G0–G4; **376 passing**. Remaining polish: an in-modal "Management key" toggle in the Keys screen (issuance already works via the API + owner).
- 2026-08-11 — **Phase 15 — LiteLLM-style Model Catalog (API + UI).** Mirrored models.litellm.ai. **Data:** vendored BerriAI/litellm `model_prices_and_context_window.json` (MIT reference data, ~3000 models / 126 providers) into `app/data/`, normalised at load and **merged** with Precepta's curated entries (so in-boundary live models keep exact matches) — fully in-boundary, no runtime external call. **API (LiteLLM-shaped):** `GET /v1/catalog/models` with filters `provider·mode·model(substring)·supports_function_calling·supports_vision·supports_reasoning` + pagination (`page/page_size`, response `{data,total_count,has_more,page,page_size}`), `GET /v1/catalog/models/{id}` single lookup, `GET /v1/catalog/providers` (counts). `catalog_lookup` made tag-aware (ollama `:3b`), so `/v1/models` enrichment stays exact. **UI:** a **Model catalog** Console screen — search box, provider dropdown (with counts), mode filter, Function-calling/Vision chips, a sortable table (Model · Provider · Mode · Max in/out · Input/Output $/1M · capability badges) and pagination — wired to the API. **Verified live:** 3011 models / 126 providers, filters (chat+vision → 777), pagination + single lookup + providers all real; catalog UI renders the table/badges/pager (headless-verified — the in-app browser is policy-blocked for localhost). +6 tests; **382 passing**; SMOKE PASS.
- 2026-08-11 — **API docs restructured (quickstart-first, tabbed) + merged into Deployment.** Followed the LiteLLM docs pattern (quickstart-first, tabbed multi-language examples, consistent OpenAI response, link to the live Swagger explorer). Removed the standalone **API** nav tab and folded it into the **Deployment** screen ("how to run it" + "how to call it" belong together): a "Use the API" section with base URL/auth, a **Quickstart tab bar (curl · Python · OpenAI SDK)** showing the same `/v1/inference` call, a compact endpoint list, and a link to `/docs`. `docs/api/README.md` rewritten to lead with the 30-second quickstart in all three languages, then add-a-provider, embeddings, catalog, observe, errors, and an "Explore next" trail. Also hid the standalone **Model catalog** screen (redundant with the Inference plane for a sovereign install; API + data kept for enrichment). JS valid; headless-verified the tabs switch languages; 382 passing.
- 2026-08-11 — **OpenAI-standard hygiene + Moderations (screening) + request/response docs.** Analysed our surface vs the OpenAI standard (the drop-in contract LiteLLM/OpenRouter follow): data plane must match exactly, control plane is legitimately extra. Fixes: (1) **slimmed `/v1/models`** to the exact OpenAI shape `{id,object,created,owned_by}` (the enriched view already lives on `/v1/endpoints`; nothing of ours read the enriched `/v1/models`); (2) added **`GET /v1/models/{model}`** (standard retrieve); (3) kept `/v1/chat/completions` canonical, `/v1/inference` as an alias. (4) **NEW `POST /v1/moderations`** — the standard content-**screening** endpoint, governed + in-boundary (firewall injection/PII + toxicity → OpenAI-shaped `{flagged,categories,category_scores}` with `precepta-guard`). (5) **Docs now show request AND response**: `docs/api/README.md` rewritten request→response for every endpoint (two-plane framing), and OpenAPI `/docs` gains 200 response examples for inference/embeddings/moderations. **Verified live:** `/v1/models` exact shape, `/v1/models/{id}` works, moderations flags injection, openapi carries the examples + new paths; drop-in (chat/completions + embeddings) unchanged. +tests (models shape, retrieve, moderations, enriched-on-endpoints); **384 passing**; SMOKE PASS.
- 2026-08-11 — **Consumer API contract: Smart Router discoverable, multi-language docs, Swagger request bodies.** Answering how consumers use the (separate) Smart Router + how to document the consumer API. (1) **Smart Router = a model, not an endpoint** — exposed `auto` / `auto:cheapest` / `auto:best-quality` as virtual models in `GET /v1/models` (owned by `precepta-router`, like OpenRouter's `openrouter/auto`), so `client.models.list()` discovers it; consumers just set `model:"auto"` on the standard `/v1/chat/completions`. (2) **Consumer docs on the frontend, under Deployment** ("Use the API" section): a **Smart Router callout**, **Quickstart tabs curl · Python · TypeScript** (OpenAI SDKs — same call, only the base URL changes), the **consumer endpoint set** (chat/completions · embeddings · moderations · models), and **Guidelines** (model selection, streaming, governance, errors, sovereignty). (3) **Swagger request bodies fixed** — handlers read the raw body so FastAPI emitted no requestBody; declared request schemas + examples via `openapi_extra` for `/v1/chat/completions`, `/v1/inference`, `/v1/embeddings`, `/v1/moderations`, so `/docs` now shows request **parameters** (with the `model` field documenting the Smart Router) alongside the response examples. `docs/api/README.md` gains a TypeScript quickstart + the Smart-Router note. +2 tests; **386 passing**; SMOKE PASS.
- 2026-08-12 — **API reference is now its own tab (OpenRouter/LiteLLM-style docs UI), not Swagger.** Replaced the compact "Use the API" block on Deployment with a dedicated **API** nav tab: a two-column docs page — sticky "On this page" section nav + a global **curl · Python · TypeScript** language switch that drives every code sample. Sections: Quickstart, Authentication, Smart Router (model:"auto"), Chat completions (params table + request + response), Embeddings, Moderations (screening), List models, Streaming, Errors, Sovereignty. Deployment keeps a one-line pointer card ("Open API reference →"). `/docs` (Swagger) remains for try-it, but the primary consumer-facing docs are now the styled in-console page. Frontend-only; console renders verified headlessly across all 3 languages + live via rebuilt container; SMOKE PASS.
- 2026-08-12 — **Full live API verification (131 checks) + BUG-002 fix.** Exercised every endpoint against the running container (proxy → app → Ollama): data plane (chat/inference/embeddings/moderations), models + Smart-Router discovery, providers/catalog, endpoints CRUD + aliases, keys lifecycle + scope gating, policies, egress, features, sovereignty controls, router config, routing/pricing/team-scopes/agent-budgets/authz, observability, assistants, and attestation/audit/compliance — plus the full **auth matrix** (anon / inference / manage:ro / manage:rw / owner). Result: **auth is correct** — invalid tokens → 401 everywhere, management gated, owner-only (sovereignty/egress/router/manage-key issuance) gated, scope matrix exact; request/response shapes match OpenAI where claimed. Found & fixed **BUG-002**: malformed chat request (missing/empty `messages`, or non-object body) returned **502** instead of **400** — now validated up front to a proper `400 invalid_request_error`. Note (by design, not a bug): the self-host default is `PRECEPTA_REQUIRE_AUTH=0` → the data plane + reference catalog are open to anonymous/any-authenticated callers on the single-tenant box; set `PRECEPTA_REQUIRE_AUTH=1` to require a key on every call. 387 tests passing; live re-verified.
- 2026-08-12 — **BUG-003: cache now works when toggled on (temperature gate fixed) + observable status.** `cache.is_cacheable` required an explicit `temperature:0`; the Playground and most OpenAI clients omit `temperature`, so caching silently never engaged. Now an **unset** temperature counts as deterministic/cacheable (explicit `temperature > 0` still opts out). Added `precepta.cache` = `hit`/`miss`/`skipped` to the response envelope so cache behaviour is visible. Live-verified: identical no-temperature request → miss then **hit** (37 tokens saved); `temperature:0.7` → skipped. Compression confirmed working as-designed (smart mode: skip <400 chars, baseline ~1k chars saved 38 tokens, aggressive ~7.7k chars saved 660 tokens) — the "nothing happens" was short-prompt skip, not a bug. 387 tests passing; console note updated to "temperature 0 or unset".
