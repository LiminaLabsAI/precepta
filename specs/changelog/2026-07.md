# Changelog — 2026-07

> One line per change, append-only.

- 2026-07-30 — Promoted scoped Phase-10 brainstorm items to backlog: FEAT-001 (key expiry + budgets), FEAT-002 (policy scope all-vs-selected), FEAT-003 (response cache exact+semantic), FEAT-004 (OpenGuard authZ/delegation adapter). Source: `BRAINSTORM.md`.
- 2026-07-30 — De-risked `open-guard-python` behind `AuthorizationPort` (DIP spike): agent/user/bounded-delegation authZ proven (fail-closed, budget+max_uses+expiry+revocation, tri-state); POC adapter satisfies the port with domain core untouched. Verdict: adopt in a dedicated phase.
- 2026-07-30 — Brainstormed Item 2 (policy scope) and Item 3 (cache) to fully-scoped; recorded decisions (all 5 scope dimensions, include-only, engine kept separate; exact+semantic cache, per-team, admin-only visibility, temperature ownership).
- 2026-07-30 — Item 4 (compression) scoped: kept as core for the non-expert/long-prompt/metered buyer; triggers = always-on (metered) + budget-pressure escalation (ties FEAT-001); local = context-fit only; observability (per-request + dashboard + net-savings formulas + quality eval); before/after captured to a consent-gated, in-boundary compression corpus (future training).
- 2026-07-30 — Critical-gap review raised cross-cutting risks: TD-001 pricing source-of-truth, TD-002 canonical metering/accounting, TD-003 streaming vs governance; plus semantic-cache go/no-go instrumentation and sovereignty-of-training (folded into FEAT-003/005). Decision: finish brainstorm → design → then work out enterprise payment.
- 2026-07-31 — Console refresh implemented on `feat/console-refresh`: rebuilt `web/console.html` (936→1170 lines) to the re-imported design — 8-nav layout, real logo (new `/assets` static mount in app/main.py). Every live surface wired to real endpoints; not-yet-built Phase-10 screens (Cache & compression, per-key budgets/expiry, policy 5-dim scope, notifications, copilot, learning) show honest "—/pending FEAT-xxx" states, never fabricated numbers. Independently browser-validated (Overview/Keys/Policies/Cache/Audit) — no JS errors, real data, design faithful. Tests 86/88 (2 SSO failures env-only: this machine's .env has OIDC; 11/11 with it unset). Not committed.
- 2026-07-30 — Re-imported the refreshed Precepta Console design from Claude Design (project aefda75e…) → `design/Precepta Console.dc.html` + `design/assets/precepta-logo.png`. Maps existing surfaces + new Phase-10 ones (Savings, key expiry/suspend, policy scope, notifications, Copilot). IMPLEMENTATION_PLAN.md + roadmap.md updated with Phase 10 (foundations → features → Console refresh → deploy).
- 2026-07-30 — Documentation brought current for the design phase: all scoped items + gaps logged to backlog (FEAT-005 compression, FEAT-006 eval harness [foundation], FEAT-007 routing, FEAT-008 traces→learning, FEAT-009 deploy; TD-004..008 for sensitivity/attribution/fail-soft/data-store-security/control-plane-governance); build-order map added to BRAINSTORM.md; status.md updated (Phase 10 brainstorm 6/7, ready for design).
- 2026-07-30 — Sensitive-data block must notify the CALLER too (not only the admin): a clear inline/API reason stating the category (never echoing the detected text). Establishes a general rule — governance blocks are always transparent to the caller (opposite of invisible cache hits).
- 2026-07-30 — Product decisions confirmed: (a) Cache defaults to EXACT-match only (similarity=1.0); semantic matching is an explicit opt-in, OFF by default — removes the fuzzy-answer liability by default. (b) Sensitive data that can't be handled compliantly is BLOCKED, not warned (fail-closed posture reaffirmed, Rule C). (c) Compression must NEVER surprise the user — no silent quality/experience change; experience-affecting compression stays opt-in + notified.
- 2026-07-30 — Item 7 (traces → learning loop) scoped: capture traces + reward signal (explicit thumbs + implicit regenerate/edit/task-outcome) → learned routing first (delivers deferred Item 5·E, contextual bandit, no GPU); model fine-tuning (RLHF/DPO) deferred. Guardrails: Rule 11 locked-evaluator-first; per-customer/in-boundary/consent-gated, never pooled. Ports: TraceSink/Feedback/Learning (DIP). Current gap: no quality/reward signal exists today.
- 2026-07-30 — Budget UX correction: at 80% Precepta NOTIFIES ONLY — never auto-downgrades the model/quality (protects UX). Cheaper routing + aggressive compression become an OPT-IN "cost-saving mode" (admin choice), whose on/off transitions fire one consolidated notification. Baseline quality-safe compression stays automatic. 100% still hard-blocks (Item 1).
- 2026-07-30 — Item 5 (advanced routing) scoped: layered decision — governance filter (C, hard fail-closed) → LLM intent-router (A, infers goal, eval-gated, governance/budget-bounded) → budget modifier (B) → failover → audit. All behind RouterBrainPort (DIP). Refinements: sensitivity auto-derived from Stage-1 firewall; C attributes = in-boundary+certified (not geography); block→admin notification (no silent fail); model-approval flow shows hosting location for conscious DPDP/GDPR consent + audit; per-request region filter, cascade (D), RL routing (E→Item 7) deferred.
- 2026-07-30 — TD-001 pricing artifact design agreed (record now, build in design phase): `model_prices` table (versioned by effective_date) + `PricingPort` DIP as single source of truth for all $ figures. Discovery: Console-added backends currently get `Price(0.0,0.0)` ⇒ $0 cost for customers' own metered endpoints.
- 2026-07-31 — TD-001 pricing artifact BUILT + verified: `app/pricing.py` (`model_prices` table, versioned by effective_date, seed for known backends) + `PricingPort`; router cheapest-routing & cost-gating, `/stats` avg cost, `/infra` price columns all sourced from it; admin `GET/POST /v1/pricing`; Console Model-plane price column (real: hf $0.60, ollama Free) + Overview avg-cost tile. Silent-$0 bug fixed (unknown price → "Set price", never a fake $0). 7 new tests; 95/95 pass; browser-validated (no JS errors).
- 2026-07-31 — TD-002 canonical metering BUILT + unit-tested: `app/metering.py` `meter()` — one definition of billable_tokens (post-compression, 0 on cache hit), budget_charge (x pricing), usage_volume (counts every request incl. cache hits), tokens_saved; canonical pipeline order documented. 4 tests. Budgets/cache/compression will all call it (no divergent counting).
- 2026-07-31 — FEAT-001 slice 1 (key expiry) BUILT + verified: api_keys.expires_at (migration), issue_key(expires_in_days=90 default, None=never), expired key fails auth (401), list_keys exposes expired/active; POST /v1/keys accepts expires_in_days; Console new-key modal Expires selector enabled + key-list shows "expires <date>"/"never expires"/Expired badge. 4 tests; 103/103 pass; browser-validated (no JS errors). Budgets slice next.
