"""Locked benchmarks (Rule 11). The router quality evaluator is `v1`:
`router_eval_v1.json` (eval set) + `judge.py` (in-boundary judge) + `harness.py`
(the mean-quality scalar). Freeze; bump to v2 to change."""
